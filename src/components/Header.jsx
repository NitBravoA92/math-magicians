import Logo from './Logo';
import Navigation from './Navigation';
import '../styles/Header.css';

const Header = () => (
  <header>
    <Logo />
    <Navigation />
  </header>
);

export default Header;
